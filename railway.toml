[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "python app.py"

[build]
  buildCommand = """
    pip install flask flask-cors "scikit-learn>=1.5.0" joblib requests python-dotenv
  """

[nixpacks]
  nixPkgs = ['python3']
  providers = ['nixpacks/python']
s = ["python311"]
